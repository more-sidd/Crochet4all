# Netlify build settings for this Vite app.
[build]
  command = "npm run build"
  publish = "dist"

# Safety net so any path falls back to the app (the app uses hash routing,
# so this is mostly belt-and-suspenders).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
